{
  "name": "scan-angka",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "node scripts/enforce-strict-scan.mjs && next dev",
    "build": "node scripts/enforce-strict-scan.mjs && next build",
    "start": "next start",
    "typecheck": "node scripts/enforce-strict-scan.mjs && tsc --noEmit"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "@vercel/analytics": "^2.0.1",
    "next": "^16.2.7",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "typescript": "^5"
  },
  "engines": {
    "node": ">=20.9.0"
  }
}
