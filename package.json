{
  "name": "scan-angka",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "typecheck": "tsc --noEmit",
    "test:movement": "node -e \"require('fs').rmSync('.movement-test',{recursive:true,force:true})\" && tsc scripts/movement-invariants.ts --outDir .movement-test --rootDir . --module commonjs --target es2020 --moduleResolution node --esModuleInterop --skipLibCheck && node .movement-test/scripts/movement-invariants.js && node -e \"require('fs').rmSync('.movement-test',{recursive:true,force:true})\""
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "@vercel/analytics": "^2.0.1",
    "next": "^16.2.7",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "typescript": "^5"
  },
  "engines": {
    "node": ">=20.9.0"
  }
}
